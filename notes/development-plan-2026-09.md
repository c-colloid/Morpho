# 開発計画（2026-09、0.18.0 時点）

外部の調査レポート（2026-09-10・リポジトリを clone して精査したもの）を受けて、
これからの 2〜4 週間と、その先の版割りを決める。
`status-and-plan.md` は 0.15.0 時点の棚卸しで、版割りの表が実態とずれたので、
**このファイルが版割りの正**。機能の設計はこれまでどおり各 notes に置く。

このファイルの「事実」はすべてリポジトリを見て確認したもの。レポートの推測は採否を明記した。

---

## 1. 現在地（確認した事実）

| 項目 | 値 |
|---|---|
| `app/app.json` / `app/package.json` の版 | **0.18.0**（2026-09-08） |
| ルート `README.md` のステータス表記 | 「現在 v0.15.0」（3 版古い → 本計画で修正） |
| `status-and-plan.md` の版割り表 | v0.16 テーマ層 / v0.17 刷り分け / v0.18 画像配置 と書いてあるが、**実際の 0.16.x は入れ子 div の修正、0.17.x はフッター、0.18.0 は iPhone とソフトキーボード**に使った |
| CI（`.github/workflows/build-ipa.yml`） | main への push で署名なし ipa を作るだけ。**`npm run check` は走っていない** |
| `npm run check` | `tsc --noEmit` + 検査スクリプト 16 本（`app/scripts/` には dump 系を含めて 22 本） |
| `app/src/ui/EditorScreen.tsx` | 2,729 行 |
| `app/src/converter/bridgeHtml.ts` | 2,914 行（WebView に注入する HTML/JS のテンプレートリテラル。tsc も lint も効かない） |
| 実機未検証の版 | 0.10.0〜0.15.0 の 6 版（`status-and-plan.md` B・受け入れ条件 1〜9）に加えて、0.16.x〜0.18.0 の 3 系統 |

0.16.1 → 0.18.0 は 8 日間で出した。フッター（0.17.0）・表のセル描画（0.17.1）・
Content with Caption のタイトル（0.17.2/0.17.3）・remount 廃止と一画面レイアウト（0.18.0）は
いずれも node 検査だけで出ており、**エディタの中核（TextInput の制御方式）を変えた 0.18.0 が
実機で動いているかは確認できていない。**

---

## 2. レポートの提言への採否

| レポートの提言 | 採否 | 理由 |
|---|---|---|
| 実機検証の一斉消化を最優先 | **採用** | 自分でも `status-and-plan.md` で「v0.16 の前に 1 周入れる」と決めていた。9 版ぶん積んだ今はなおさら |
| README の版表記を直し、再発を仕組みで防ぐ | **採用** | 版は `app.json` の 1 箇所が正。README は版番号を書かず変更履歴へのリンクだけにする（機械同期より「書かない」が確実） |
| CI で `check` を回しているか未確認（推測） | **確認済み: 回していない** → **採用（追加する）** | ipa ビルドは macOS runner で 60 分かかるが、`check` は Node だけで数分。PR と push で回す別ジョブにする |
| 案A: カーソル単位の部分変換 | **保留（計測が先）** | CLAUDE.md の判断は「文書長が効いてきてから足す」。50 枚 47 ms の実測に対し、まだ誰も長文書で困っていない。先に**端から端の遅延**（後処理・展開・解析・描画込み）を測り、文書長との関係を数字で持ってから決める |
| 案B: bridgeHtml の正規表現パーサを DOMParser に置換 | **見送り** | レポート自身も様子見を推奨。回帰リスクが 20 件の落とし穴対処すべてに及ぶ。ただし bridgeHtml を**テンプレートリテラルから分離して型検査が効く形にする**ことは別件として採用（下記 3-C） |
| 案C: EditorScreen からプレビュー同期を切り出す | **部分採用** | 全面リファクタリングはしない。テーマ層で触る範囲（変換トリガー・カーソル対応・装飾オーバーレイ）だけを hook に出す |
| テーマ層（第2層）の着手 | **採用（版番号を振り直す）** | v0.16 は使ってしまったので **v0.19** に |
| PDF の 1 回の実験 | **採用（前倒し）** | 半日で済み、結果次第で README の「PDF / epub 書き出し」の書き方が変わる |
| 変換器は pandoc.wasm 継続、自作は先送り | **採用（現状維持）** | CLAUDE.md の結論と同じ |
| 「pandoc では実現不可能な組版要件」の一覧化 | **採用（テーマ層の設計に含める）** | 縦書き・ルビの精密制御など。自作 writer への切り替えトリガーをここで持つ |
| pandoc.wasm 同梱の技術検証を先行 | **採用（オフライン起動の確認とセット）** | ライセンス判断は配布の瞬間まで先送りのまま |
| 落とし穴カタログの線形増加が保守コスト（推測） | **一部同意** | 件数は増えるが、各項目は「検出 → 診断」の 1 行で済んでいる。作り込みすぎない方針（CLAUDE.md）を維持し、lint を製品機能にしない |

---

## 3. 直近 2〜4 週間の作業

順番に意味がある。**1 と 2 が終わるまで新機能は積まない。**

### 3-A. 即日（コードに触らない）

- [x] `README.md` の版表記を直す（版番号を消して変更履歴へのリンクにする）
- [x] `status-and-plan.md` の冒頭に「版割りはこのファイルが正」の一行を足す（本計画と同時）

### 3-B. CI に `check` ジョブを足す（半日）— **済（0.18.1）**。設計と検証は `foundation-2026-09.md` A

- `build-ipa.yml` とは別のワークフロー `check.yml`。`ubuntu-latest`・Node 22・`npm ci && npm run check`
- トリガーは `pull_request` と `push`（`app/**` の変更時）。ipa ビルドと違って main 以外でも走らせる
- 検査スクリプトが pandoc.wasm を CDN から取っているなら、runner のネットワークで通るかを最初に確認する。
  通らなければ wasm を actions/cache に載せる（55.9 MB。キャッシュ上限内）
- 受け入れ: PR を開いたら数分で緑赤が付く。`fixtures/pptx-benchmark.md` は触らない

### 3-C. 実機周回（1〜2 週間。patch は 0.18.x）

`status-and-plan.md` の受け入れ条件 1〜9 と「0.18.0 の確認」1〜5 を **順序を変えて**回す。
0.18.0 の TextInput の変更が壊れていると、それより前の検証の結果が信用できないため。

1. **0.18.0 の確認 1・2・5**（ソフトキーボードとツールバー・物理キーボードのバー・スクロールで focus しない）
2. **0.18.0 の確認 4**（画像・段組み・出典・ノート・改行編集の書き戻しでキャレットが飛ばない）—
   remount 廃止の答え合わせ。ここが赤なら他に進まない
3. 受け入れ条件 **9**（0.15.0 `+++`・全角 `＋＋＋`・3 列警告・列頭画像の診断）と **8**（0.14.0 段組みの縦位置・列内の表）
4. 受け入れ条件 **7**（ルビ・傍点・画像を pptx / docx / Web で書き出し、PowerPoint / Word / ブラウザで開く）—
   0.17.x のフッターと Content with Caption のタイトルもこの周で PowerPoint で見る
5. 受け入れ条件 **1・2・5**（bookmark 自動再接続・装飾パネルとスクロール・テンプレート）
6. 受け入れ条件 **3・4** と 0.18.0 の確認 3（スライドショーの端スワイプ・回転・Slide Over）

出た不具合は 0.18.1, 0.18.2, … の patch で潰す。過去の実績（機能 1 つに patch 2〜4 本）から、
**9 版ぶんで 10〜20 本の patch を見込む。** 2 週間で消えなければ、残りは
「実機で見た症状」を `status-and-plan.md` B に書き足して v0.19 と並走させる。

### 3-D. 計測（3-C と並走。各半日）— PDF は **済（出せない。`foundation-2026-09.md` D）**。残り 3 件は実機

`status-and-plan.md` B の「測っていないもの」を数字にする。結論は CLAUDE.md の実測値表へ追記する。

| 計測 | 目的 | 決まること |
|---|---|---|
| 端から端の遅延（入力停止 → 画が出るまで。後処理・展開・解析・描画込み）を 10 / 50 / 150 枚で | 部分変換（案A）の要否 | 150 枚で 300 ms を超えるなら v0.20 で部分変換。超えないなら見送り継続 |
| 画像を数枚含む文書で連続編集したときのヒープ | 115 MB は画像なしの値 | 画像の総量に上限を設けるか |
| 機内モードで起動 | README の「初回のみネット接続が必要」が本当か | 同梱（WKURLSchemeHandler）の優先度 |
| `to: 'pdf'` を pandoc.wasm に投げる | PDF が原理的に可能か | README の「PDF / epub」の表現。不可なら「pdf は pptx / docx を Files 経由で」と書き換える |

### 3-E. テーマ層に向けた足場 — **済（0.18.1）**。この環境で進められたので 3-C を待たずに出した。
実機での確認は 3-C の周回に含める（`foundation-2026-09.md` C）

- `EditorScreen.tsx` からプレビュー同期（変換トリガー・カーソル ⇔ スライド・装飾オーバーレイの座標）を
  `usePreviewSync` 系の hook に出す。**動作を変えない**（`npm run check` が緑のまま）
- `bridgeHtml.ts` の中身を `app/src/converter/bridge/*.js` の実ファイルに分け、ビルド時に文字列へ束ねる
  （テンプレートリテラルの「`` ` `` と `${` を使うな」制約を外し、tsc / lint を効かせる）。
  DOMParser への置換はしない。文字列の出し方を変えるだけ
- 「pandoc では実現できない組版要件」の一覧を `theme-layer.md`（新規）に書く。
  縦書き・ルビの位置制御・傍点の種類・禁則・段組みの列比。各項目に「テーマ層で吸収できるか / writer の差し替えが要るか」を付ける

---

## 4. 版割り（振り直し）

`status-and-plan.md` の表を置き換える。中身は変えず、番号だけ 3 つ繰り下げる。

| 版 | 中身 | 実機に届くもの |
|---|---|---|
| **0.18.x** | 3-C の実機周回の patch。**0.18.1 は足場の整理（CI・ブリッジ分割・プレビュー同期の hook 化）で、周回の前に出した** | 9 版ぶんの手当て |
| **v0.19 テーマ層** | 第2層の本体（`roadmap-pptx.md`）。意味クラスのコンパイラ・列比・組み込みテーマ 1 本。縦書きとインスペクタ v1 の席。**0.19.0 で骨格を出した**（`theme-layer.md` の実装の節。実機未検証・3-C の周回に含める） | 段組みの列比が変えられる。見た目が変わる |
| **v0.20 刷り分け** | docx / epub・Web への段組みの落とし方、書き出し前の劣化告知、`headingSegments`（文書プレビューのカーソル同期）。3-D の結果次第で部分変換 | 「この配置は Word では 1 本の流れになります」が出る |
| **v0.21 画像配置** | 画像の当たり判定・`Placement`・`<p:pic>` の xfrm 上書き・全画面の配置モード。アンカー（案A/案B）の決着 | 画像の大きさ・位置をタッチで決められる |
| **v0.22 テーマの共有** | テーマの編集 UI・書き出し・読み込み。文字サイズと配線盤の移設 | テーマを文書間で使い回せる |
| **v0.23 日本語組版** | 縦書き（3 形式 + プレビュー描画）・圏点の種類・ルビの揃え・縦中横・字下げ | 縦書きの原稿がスライドと書籍に刷り分けられる |
| **v0.24 PDF** | Typst の WASM を第 2 のエンジンとして同梱。CJK フォント | PDF が端末内で出る |

v0.19 の着手は **3-C の 1〜3 が緑になってから**。2〜4 週間の目標は
「テーマのスキーマ（YAML）と pptx 向けコンパイラの骨格・組み込みテーマ 1 本で列比が変わる」まで。

---

## 5. 版に紐づかない宿題

- **pandoc.wasm の同梱とライセンス判断。** 3-D のオフライン計測で「機内モードで起動できない」と出たら、
  同梱の技術検証（WKURLSchemeHandler・`fetchWasm()` の差し替え）だけ先に進める。
  App Store に出す判断はそのときまで先送りのままでよい（CLAUDE.md）
- **ドキュメントの版ずれの再発防止。** README に版番号を書かない。`status-and-plan.md` は
  棚卸しのたびに冒頭の版を更新し、版割りの表は本ファイルへ寄せる
- `fixtures/pptx-benchmark.md` は書き換えない

---

## 6. リスク

| リスク | 兆候 | 手当て |
|---|---|---|
| 0.18.0 の remount 廃止が実機で壊れている | 3-C の 2 で書き戻し後にキャレットが飛ぶ・IME の未確定文字が消える | 0.18.x の patch で `value=` 制御を戻し、差し込みだけ remount に戻す退路を残す |
| 実機周回の patch が 2 週間で終わらない | 3-C の 4 以降が着手できない | 残りを B に書き足して v0.19 と並走。周回を止めて v0.19 だけに寄せない |
| CI の `check` が CDN 取得で不安定 | ジョブが赤になるが再実行で緑 | wasm を actions/cache に載せる。それでも駄目なら `check` を wasm 不要の検査とそれ以外に分ける |
| bridgeHtml の分割で束ね方を間違える | `check-bridge.mjs` が赤 | 束ねた文字列の sha256 を分割前と比較する検査を最初に書く |
| テーマ層のスキーマを列比だけで設計してしまう | `roadmap-pptx.md` の注意 | 3-E の一覧（縦書き・ルビ・docx の資材）を先に書いてからスキーマを切る |

---

## 7. 未実装機能の計画（0.19.1 時点の棚卸し）

0.18.1〜0.19.1 で 3-B / 3-D（PDF）/ 3-E とテーマ層の骨格・フッターの位置と色が入った。
残りの未実装を**版ごとに**まとめる。各行の「規模」は 0.6〜0.17 の実績（1 版 ≒ 500 行前後 +
検査 + 実機 patch 2〜4 本）に合わせた目安。**実機が要るもの**は iPad の周回（3-C）と
同時にしか閉じないので、「この環境で先に進められるか」を分けて書く。

### 7-0. 前提（版ではない）— 3-C 実機周回

0.10.0〜0.19.1 の**10 版分が実機未検証**。とくに 0.18.0（TextInput の制御方式）と
0.19.0（列比・意味クラス）・0.19.1（フッターの位置と色）は実機でしか正否が出ない。
順序は 3-C のとおり。**v0.20 以降の着手条件は 3-C の 1〜3 が緑になること**（変わらず）。

| 実機で見ること（追加分） | 版 |
|---|---|
| 装飾パネルの「テーマ」で列比を替えて 2 段の幅が変わる／`[語]{.accent}` が PowerPoint・Word・ブラウザで色付き | 0.19.0 |
| フッターを上下へ動かして装飾の帯から逃げる／白文字が濃い帯の上で読める／書き出しの pptx でも同じ位置・色 | 0.19.1 |
| hook 化後に形式切替・テンプレート変更・画像挿入でプレビューが取り直される | 0.18.1 |

### 7-1. v0.20 刷り分け

「一つの原稿から刷り分ける」の欠けを埋める版。docx / Web での見え方の差を**書き手に知らせる**ことが主。

| 中身 | 設計の出どころ | この環境で | 検証 | 規模 |
|---|---|---|---|---|
| `headingSegments`（見出し区間への一般化）。文書・Web プレビューにカーソル同期を付ける | `preview-formats.md` | 進められる | `check-cursor` に区間の一致 | 中 |
| 書き出し前の劣化告知。「段組みは Word では 1 本の流れ」「3 列目はスライドに出ない」を書き出しメニューに出す | `columns-and-images.md` v0.17 | 進められる | 診断の単体 | 小 |
| docx の「並置を保つ」（罫線なしの 1×N 表。既定にしない明示オプション） | `columns-and-images.md` docx | 進められる | `check-deck` に docx の表 | 小〜中 |
| pandoc ネイティブ記法の正規化（全角波括弧・ドット忘れ。62 通り中 26 件を救う） | `column-input.md`「救済」 | 進められる（試作済み） | `check-columns` に 62 ケース + 誤爆 10 | 中 |
| epub 書き出し（`to: 'epub'` が wasm で通るかの実験 → 通れば書き出しメニューへ） | `theme-layer.md` 4 | **実験は半日で済む** | `check-deck` | 小（実験）〜中 |
| 部分変換（カーソルのスライドだけ）— **3-D の遅延計測の結果次第。150 枚で 300 ms を超えたら** | CLAUDE.md 性能設計 | 計測は実機 | — | 中 |

### 7-2. v0.21 画像配置

ライブプレビューの hot path に初めて OOXML 後処理を持ち込む版。**3-D の遅延計測が前提**。

| 中身 | 設計の出どころ | この環境で | 検証 | 規模 |
|---|---|---|---|---|
| `Placement`（画像の大きさ・位置。`loadDesign` / `parseDesignFile` / `serializeDesign`） | `columns-and-images.md` v0.18 | 進められる | `check-design` に往復 | 小 |
| `<p:pic>` の xfrm 上書きと z 順（`descr` の最終行 + 出現順で特定）。`doConvert` / `doExport` の両方 | 同上 | 進められる | `check-deck` に注入後の解析（往復） | 中 |
| 1 枚だけの列比の上書き（slide 経路。`<p:ph idx>` を特定して `<a:xfrm>` を注入） | 同上・`theme-layer.md` 実装の残り | 進められる | `check-theme` | 小 |
| 画像の当たり判定（`DecorEditLayer` の分担を画像へ一般化） | 同上 UI | 書ける・**実機で確かめる** | — | 中 |
| 全画面の配置モード（`SlideShow` の stage + `DecorEditLayer`。横ページングと端タップを止める・ピンチ） | 同上・`roadmap-pptx.md` 検討メモ | 書ける・**実機必須** | — | 中〜大 |
| アンカー（案A `{#s-xxxx}` / 案B 見出しハッシュ）の決着と、外れた配置の再リンク UI | `roadmap-pptx.md` | 判断はここで | `check-design` | 小 |
| 後処理込みの遅延をヘッダに内訳表示 | `columns-and-images.md` | 進められる | — | 小 |

### 7-3. v0.22 テーマの共有

0.19.0 の骨格を「文書間で使い回せる」まで持っていく版。

| 中身 | 設計の出どころ | この環境で | 検証 | 規模 |
|---|---|---|---|---|
| `.morphotheme`（JSON）の書き出し・読み込み。組み込みテーマを複製して編集の起点にする | `theme-layer.md`・`columns-and-images.md` v0.19 | 進められる | `check-theme` に往復 | 小 |
| テーマの編集 UI（列比・意味クラスの色と太字・名前）。ヘッダに「テーマ」の入口を新設 | `columns-and-images.md` UI | 書ける・実機で使用感 | — | 中 |
| 文字サイズ・帯モード・配線盤・フッター体裁を**テーマ側へ移せる**ようにする（文書側の値は上書き） | 同上 | 進められる | `check-design` / `check-theme` | 中 |
| セレクタ規則 `rules[]`（「タイトルスライドに帯」「h1 下にアクセント線」を装飾プリセットから生成） | `roadmap-pptx.md` 三層分離 | 進められる | `check-design` | 中 |
| インスペクタ v1（プレビューの語をタップして `[語]{.accent}` を挿入。`patchBody` 経由） | `roadmap-pptx.md` v0.4 | 書ける・**実機必須**（IME との同居） | `check-image-insert` と同型の挿入検査 | 中 |
| `patchBody` の 1 段 Undo と楽観ロックの共通化（段組み挿入・インスペクタが重い編集になるため） | `columns-and-images.md` UI | 進められる | `check-notes-edit` | 小〜中 |

### 7-4. v0.23 日本語組版

差別化の本丸。**アプリのプレビュー（自前の実寸描画）が最大の実装量**で、変換器側は T で届く。

| 中身 | 経路（`theme-layer.md`） | この環境で | 検証 | 規模 |
|---|---|---|---|---|
| 縦書きの実験 3 件: pptx `a:bodyPr vert="eaVert"` の後処理／docx `w:textDirection tbRlV`／epub の `page-progression-direction` | T（未検証） | **各半日。先に済ませる** | Node で実出力 | 小 |
| 縦書き（`Theme.writingMode`）を 3 形式へ落とす | T | 進められる | `check-theme` | 中 |
| プレビューの縦組み描画（`SlideSurface` / `DocumentSurface`。RN の Text は縦書き不可 → 1 字ずつ置く。句読点・長音の向き） | 描画側 | 書ける・**実機で見る** | 単体は難しい。実機 | **大** |
| 圏点の種類（`emphasis.dotStyle`）・ルビの揃え（`ruby.align`）・縦中横 | T（Lua の属性） | 進められる | `check-deck` / `check-theme` | 小〜中 |
| 字下げ（段落頭 1 字）。禁則はプレビューの折り返しに近似を入れる | T / 描画側 | 進められる | `check-scene` | 中 |
| 傍点の pptx 近似を「親文字の上に `•` を置く図形」へ（現状は太字） | T（未検証） | 実験して決める | `check-deck` | 中 |

### 7-5. v0.24 PDF（第 2 のエンジン）

| 中身 | 根拠 | この環境で | 規模 |
|---|---|---|---|
| Typst の WASM ビルド（typst.ts 等・Apache-2.0）を同じ WebView に載せ、pandoc の `to: 'typst'` 出力を PDF に組む。`Converter` の中に閉じる | `foundation-2026-09.md` D（pandoc.wasm は PDF を出せない・実測） | 進められる（Node で実験可） | 中〜大 |
| CJK フォントの同梱（WASM FS）。サイズとライセンスの判断 | CLAUDE.md 未検証項目 | 判断 | 小 |
| 縦書き PDF（Typst の縦組みは限定的。**未検証**） | — | 実験 | ? |

### 7-6. 版に紐づかない宿題（変わらず）

- pandoc.wasm の同梱（WKURLSchemeHandler）とライセンス判断。**3-D のオフライン計測が引き金**
- 全画面の装飾編集モードを作るかの判断（0.6.3 の保留）。v0.21 の配置モードで決着する
- iPhone 実機での確認（0.18.0）

### 順序の理由

1. **v0.20 を先にする**のは、この環境で進められる項目が多く（5 件中 4 件）、実機周回と並走できるから。
   epub と正規化は「実験済み・設計済み」で着手コストが最も低い
2. **v0.21 は 3-D の遅延計測の後**。hot path に後処理を足す版なので、数字なしに進めない
3. **v0.22 を v0.23 より先にする**のは、縦書きの席（`writingMode`）と規則（`rules[]`）が
   テーマファイルに載っていないと、縦書きを入れた瞬間にテーマの書式が変わるから
4. **v0.23 の実験 3 件は前倒ししてよい**（各半日）。結果がテーマのスキーマに影響する
5. **v0.24 は最後**。製品の主役はエディタで、PDF は pptx / docx を Files 経由で開く逃げ道がある

### この環境で今すぐ進められるもの（順に）

1. v0.23 の縦書き実験 3 件と v0.20 の epub 実験（合わせて 2 日。すべて Node の実測で決着する）
2. v0.20 の劣化告知・正規化・`headingSegments`・docx 並置（1〜2 週間）
3. v0.21 の `Placement` と xfrm 注入・slide 経路の列比（後処理の骨格。UI は実機待ち）
